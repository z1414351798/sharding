<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.z.sharding.mapper.RegionMapper">

    <insert id="insert" parameterType="com.z.sharding.pojo.Region">
        INSERT INTO t_region(id, name , description, create_time, update_time)
        VALUES (#{id}, #{name}, #{description}, #{createTime}, #{updateTime})
    </insert>

    <select id="getById" parameterType="int" resultType="com.z.sharding.pojo.Region">
        SELECT
        id AS id,
        name AS name,
        description AS description,
        create_time AS createTime,
        update_time AS updateTime
        FROM t_region
        WHERE id = #{id}
    </select>


</mapper>
